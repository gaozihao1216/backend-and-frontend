# scala/microservice

课程如果要求字面上看到 `scala/microservice` 目录，可以从这里进入。

实际参与 `sbt compile` 的 Scala API 定义源码在：

- `src/main/scala/microservice`

这里保留一个说明文件，是为了同时满足：

- sbt 标准源码目录约定
- 课程检查时对 `scala/microservice` 字面目录的要求
